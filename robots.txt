User-agent: *
Allow: /

# Keep design proposals out of the index.
# /thanks is noindex via meta tag — NOT disallowed here, because
# a robots.txt Disallow would prevent Google from crawling the
# page to see the noindex meta (Google's recommendation: pick one).
Disallow: /proposals/

Sitemap: https://toromovers.net/sitemap.xml
